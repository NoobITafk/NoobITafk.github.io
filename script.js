const isEN=document.documentElement.lang==='en';
const AVAILABLE=true;
const modal=document.getElementById('contactModal');
const dialog=modal?.querySelector('.dialog');
const openButtons=[...document.querySelectorAll('.contact-open')];
const closeButtons=[...document.querySelectorAll('.contact-close')];

const starSvg=(filled=true)=>`<svg viewBox="0 0 24 24" aria-hidden="true"><path d="M12 2.9l2.72 5.52 6.09.88 4.4 4.3-1.04 6.07L12 16.8 6.55 19.67l1.04-6.07-4.4-4.3 6.09-.88L12 2.9z" fill="${filled?'currentColor':'none'}" stroke="currentColor" stroke-width="1.7" stroke-linejoin="round"/></svg>`;

const style=document.createElement('style');
style.textContent=`
.hero .hero-choices{display:flex!important;flex-wrap:wrap!important;align-items:center!important;gap:7px!important;margin-top:15px!important}.hero .hero-choices>span:first-child{font:500 10px/1.2 'DM Sans',sans-serif!important;color:#85847d!important;margin-right:2px!important}.hero .hero-choices>a{display:inline-flex!important;align-items:center!important;gap:6px!important;padding:6px 9px!important;border:0!important;border-radius:999px!important;background:#eeece6!important;color:#4b4a45!important;font:700 10px/1 'DM Sans',sans-serif!important;transition:transform .16s ease,background .16s ease,color .16s ease!important}.hero .hero-choices>a:hover{background:#fff!important;color:#3f5ee8!important;transform:translateY(-2px)!important}.hero .hero-choices .brand-mark{width:18px!important;height:18px!important;flex-basis:18px!important;border-radius:6px!important;font-size:7px!important}.hero .profile:before,.hero .hero-aside:before{display:none!important}.hero .profile img,.hero .portrait{box-shadow:0 18px 42px rgba(20,20,16,.10)!important}
@media(max-width:600px){.hero .hero-choices{gap:6px!important;margin-top:13px!important}.hero .hero-choices>a{padding:6px 8px!important}.hero .hero-choices>span:first-child{width:100%!important;margin-bottom:1px!important}}
`;
document.head.appendChild(style);

function applyAvailability(){document.querySelectorAll('.profile p').forEach(p=>{if(!p.querySelector('em'))return;p.classList.add('status-pill');p.classList.toggle('busy',!AVAILABLE);p.innerHTML=`<em></em>${isEN?(AVAILABLE?'Open to new projects':'Currently unavailable'):(AVAILABLE?'Відкритий до нових проєктів':'Тимчасово недоступний для нових проєктів')}`})}

function addMobileLanguage(){if(document.querySelector('.mobile-language'))return;const el=document.createElement('div');el.className='mobile-language';el.innerHTML=`<a href="/" class="${isEN?'':'active'}">UA</a><a href="/en/" class="${isEN?'active':''}">EN</a>`;document.body.appendChild(el)}

function addProfileReview(){const profile=document.querySelector('.profile');if(!profile||profile.querySelector('.profile-review'))return;const box=document.createElement('div');box.className='profile-review';box.innerHTML=`<div class="profile-review-head"><strong class="profile-review-title">${isEN?'Client reviews':'Відгуки клієнтів'}</strong><span class="profile-review-stars">${starSvg()}${starSvg()}${starSvg()}${starSvg()}${starSvg()}</span></div><div class="profile-review-meta"><small>${isEN?'A review can be left after the project.':'Відгук можна залишити після завершення проєкту.'}</small><button type="button" class="review-open"><span>${starSvg(false)}</span>${isEN?'Leave review':'Залишити відгук'}</button></div>`;profile.appendChild(box);box.querySelector('.review-open').addEventListener('click',openReview)}

function applyHeroCopy(){const hero=document.querySelector('.client-hero')||document.querySelector('.hero');if(!hero)return;const h=hero.querySelector('h1');const lead=hero.querySelector('.lead');const primary=hero.querySelector('.primary');const secondary=hero.querySelector('.secondary');if(isEN){if(h)h.innerHTML='Have a task?<br><i>I’ll make it work.</i>';if(lead)lead.textContent='Tell me what you need to build, automate or improve. You do not need to know the technical side — describe the result and I’ll turn it into a clear solution.';if(primary)primary.innerHTML='Tell me what you need <span>→</span>';if(secondary)secondary.innerHTML='See the work <span>↓</span>'}else{if(h)h.innerHTML='Є завдання?<br><i>Зроблю, щоб воно працювало.</i>';if(lead)lead.textContent='Розкажіть, що потрібно створити, автоматизувати або виправити. Технічні деталі знати не потрібно — опишіть бажаний результат, а я допоможу знайти просте рішення.';if(primary)primary.innerHTML='Розповісти про задачу <span>→</span>';if(secondary)secondary.innerHTML='Подивитися роботи <span>↓</span>'}}

function openModal(){if(!modal)return;modal.classList.add('open');modal.setAttribute('aria-hidden','false');document.body.style.overflow='hidden'}
function closeModal(){if(!modal)return;modal.classList.remove('open');modal.setAttribute('aria-hidden','true');document.body.style.overflow=''}

function openContact(){if(!modal||!dialog)return;dialog.className='dialog contact-dialog';dialog.innerHTML=`<button class="close contact-close" type="button" aria-label="Close">×</button><p>${isEN?'CONTACT':'КОНТАКТ'}</p><h2>${isEN?'Tell me what you need.':'Розкажіть, що потрібно.'}</h2><span class="sub">${isEN?'A few lines are enough. No technical brief required.':'Достатньо кількох слів. Технічне ТЗ не потрібне.'}</span><form class="contact-form" id="siteContactForm"><div><label>${isEN?'Your name':'Ваше ім’я'}</label><input name="name" required autocomplete="name" placeholder="${isEN?'Name':'Ім’я'}"></div><div><label>${isEN?'Your email':'Ваш email'}</label><input name="email" type="email" required autocomplete="email" placeholder="name@example.com"></div><div><label>${isEN?'What do you need?':'Що потрібно зробити?'}</label><textarea name="message" required placeholder="${isEN?'Website, bot, automation, integration…':'Сайт, бот, автоматизація, інтеграція…'}"></textarea></div><input type="hidden" name="_subject" value="${isEN?'New portfolio inquiry':'Нове звернення з портфоліо'}"><input type="hidden" name="_template" value="table"><button class="contact-submit" type="submit">${isEN?'Send message →':'Надіслати повідомлення →'}</button><div class="contact-status" id="contactStatus"></div><p class="contact-hint">${isEN?'Email is sent without leaving the site. Telegram opens with your message ready to send.':'Email надсилається без виходу із сайту. Telegram відкриється з уже готовим повідомленням.'}</p></form><div class="contact-alt"><button type="button" data-direct="telegram">Telegram</button><a href="https://www.fiverr.com/enotik_" target="_blank" rel="noreferrer">Fiverr</a><a href="https://freelancehunt.com/ua/showcase/work/freelance-helper-telegram-bot-dlya/2046690.html" target="_blank" rel="noreferrer">Freelancehunt</a></div>`;bindContact();openModal();setTimeout(()=>dialog.querySelector('textarea')?.focus(),120)}
function bindContact(){dialog.querySelectorAll('.contact-close').forEach(b=>b.addEventListener('click',closeModal));const form=dialog.querySelector('#siteContactForm');const status=dialog.querySelector('#contactStatus');form?.addEventListener('submit',async e=>{e.preventDefault();const button=form.querySelector('.contact-submit');button.disabled=true;button.textContent=isEN?'Sending…':'Надсилаю…';status.className='contact-status';const data=Object.fromEntries(new FormData(form).entries());try{const r=await fetch('https://formsubmit.co/ajax/maks2006bl@gmail.com',{method:'POST',headers:{'Content-Type':'application/json','Accept':'application/json'},body:JSON.stringify(data)});if(!r.ok)throw new Error();form.reset();status.textContent=isEN?'Sent. I’ll get back to you soon.':'Надіслано. Я зв’яжуся з вами найближчим часом.';status.className='contact-status show ok'}catch{status.textContent=isEN?'Could not send. Try Telegram instead.':'Не вдалося надіслати. Спробуйте Telegram.';status.className='contact-status show err'}finally{button.disabled=false;button.textContent=isEN?'Send message →':'Надіслати повідомлення →'}});dialog.querySelector('[data-direct="telegram"]')?.addEventListener('click',()=>{const data=new FormData(form);const name=String(data.get('name')||'').trim();const email=String(data.get('email')||'').trim();const message=String(data.get('message')||'').trim();const text=isEN?`Hi Maksym!\n\nName: ${name}\nEmail: ${email}\n\n${message}`:`Привіт, Максиме!\n\nІм’я: ${name}\nEmail: ${email}\n\n${message}`;window.open('https://t.me/MaksBlischik?text='+encodeURIComponent(text),'_blank','noopener,noreferrer')})}

function openReview(){if(!modal||!dialog)return;dialog.className='dialog review-dialog';dialog.innerHTML=`<button class="close review-close" type="button" aria-label="Close">×</button><p>${isEN?'FEEDBACK':'ВІДГУК'}</p><h2>${isEN?'How was the cooperation?':'Як вам співпраця?'}</h2><span class="sub">${isEN?'Choose a rating and leave a few words. The review will be sent to me for approval before publication.':'Оберіть оцінку та залиште кілька слів. Відгук спочатку надійде мені на перевірку.'}</span><form class="review-form" id="reviewForm"><div><label>${isEN?'Your name':'Ваше ім’я'}</label><input name="review_name" required placeholder="${isEN?'Name':'Ім’я'}"></div><div><label>${isEN?'Rating':'Оцінка'}</label><div class="review-stars" role="radiogroup">${[1,2,3,4,5].map(n=>`<button type="button" data-rate="${n}" aria-label="${n} stars">${starSvg(false)}</button>`).join('')}</div><input id="reviewRating" type="hidden" name="rating" value="5"></div><div><label>${isEN?'Your review':'Ваш відгук'}</label><textarea name="review" required placeholder="${isEN?'What did you like about working together?':'Що вам сподобалося у співпраці?'}"></textarea></div><input type="hidden" name="_subject" value="${isEN?'New portfolio review':'Новий відгук з портфоліо'}"><input type="hidden" name="_template" value="table"><button class="review-submit" type="submit">${isEN?'Send review →':'Надіслати відгук →'}</button><div class="review-status" id="reviewStatus"></div><p class="review-note">${isEN?'Reviews are checked before publication.':'Перед публікацією відгуки перевіряються.'}</p></form>`;const stars=[...dialog.querySelectorAll('[data-rate]')];const rating=dialog.querySelector('#reviewRating');let selected=5;const paint=()=>stars.forEach((b,i)=>{b.classList.toggle('active',i<selected);b.innerHTML=starSvg(i<selected)});paint();stars.forEach(b=>b.addEventListener('click',()=>{selected=Number(b.dataset.rate);rating.value=selected;paint()}));dialog.querySelector('.review-close').addEventListener('click',closeModal);const form=dialog.querySelector('#reviewForm');const status=dialog.querySelector('#reviewStatus');form.addEventListener('submit',async e=>{e.preventDefault();const btn=form.querySelector('.review-submit');btn.disabled=true;btn.textContent=isEN?'Sending…':'Надсилаю…';const data=Object.fromEntries(new FormData(form).entries());try{const r=await fetch('https://formsubmit.co/ajax/maks2006bl@gmail.com',{method:'POST',headers:{'Content-Type':'application/json','Accept':'application/json'},body:JSON.stringify(data)});if(!r.ok)throw new Error();form.reset();status.textContent=isEN?'Thank you. Your review was sent for approval.':'Дякую. Відгук надіслано на перевірку.';status.className='review-status show ok'}catch{status.textContent=isEN?'Could not send the review.':'Не вдалося надіслати відгук.';status.className='review-status show err'}finally{btn.disabled=false;btn.textContent=isEN?'Send review →':'Надіслати відгук →'}});openModal()}

function addMobileLanguage(){if(document.querySelector('.mobile-language'))return;const el=document.createElement('div');el.className='mobile-language';el.innerHTML=`<a href="/" class="${isEN?'':'active'}">UA</a><a href="/en/" class="${isEN?'active':''}">EN</a>`;document.body.appendChild(el)}

applyAvailability();addMobileLanguage();addProfileReview();applyHeroCopy();
openButtons.forEach(b=>b.addEventListener('click',openContact));
closeButtons.forEach(b=>b.addEventListener('click',closeModal));
document.addEventListener('keydown',e=>{if(e.key==='Escape')closeModal()});
const links=[...document.querySelectorAll('nav a[href^="#"]')];const sections=links.map(a=>document.querySelector(a.getAttribute('href'))).filter(Boolean);if('IntersectionObserver'in window&&sections.length){const io=new IntersectionObserver(entries=>{const visible=entries.filter(x=>x.isIntersecting).sort((a,b)=>b.intersectionRatio-a.intersectionRatio)[0];if(visible)links.forEach(a=>a.classList.toggle('active',a.getAttribute('href')==='#'+visible.target.id))},{rootMargin:'-20% 0px -65% 0px',threshold:[0,.1,.3,.6]});sections.forEach(s=>io.observe(s))}
const revealTargets=document.querySelectorAll('.how-grid article,.project,.capabilities article,.platform');if('IntersectionObserver'in window&&revealTargets.length){revealTargets.forEach((el,i)=>{el.classList.add('reveal-on-scroll');el.style.setProperty('--delay',`${Math.min(i*35,210)}ms`)});const rio=new IntersectionObserver(entries=>entries.forEach(entry=>{if(entry.isIntersecting){entry.target.classList.add('is-visible');rio.unobserve(entry.target)}}),{threshold:.12});revealTargets.forEach(el=>rio.observe(el))}
